<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Yoga Fitness Club", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                @if (User.IsInRole("User"))
                {
                    <li>@Html.ActionLink("Customer Profile", "Index", "Customers")</li>
                }
                @if (User.IsInRole("Tutor"))
                {
                    <li>@Html.ActionLink("Instructor Profile", "Index", "Instructors")</li>
                    <li>@Html.ActionLink("Schedule Manager", "Index", "SessionScheduler")</li>
                }
               
               @if (User.IsInRole("Admin"))
                {
                    <li>@Html.ActionLink("Role Manager", "Index", "Roles")</li>
                }   
                
                <li>@Html.ActionLink("Contact", "Contact", "Home")</li>

            </ul>
            @Html.Partial("_LoginPartial")
        </div>
    </div>
</div>

